/**
 * 聚合广告 SDK - 类型统一导出
 */

const { AdPlatform, AdType, AdEvent, AdErrorCode, DevicePlatform, KeyCode, KeyAction } = require('./enums');

module.exports = {
  AdPlatform,
  AdType,
  AdEvent,
  AdErrorCode,
  DevicePlatform,
  KeyCode,
  KeyAction,
};
